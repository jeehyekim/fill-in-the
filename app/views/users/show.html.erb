<div class="container">
      <!-- left column -->
      <div class="col-md-3 hidden-sm hidden-xs">
        <div class="text-center navigation-left">
          <% if !@user.image %>
            <img src="//placehold.it/200" class="avatar img-circle" alt="avatar">
          <% else %>
           <%= image_tag @user.image.url(:thumb) %>
          <% end %>
          <h2>Welcome, <%= @user.first_name %></h2>
          <h5>Joined: <%= time_ago_in_words(@user.created_at) %> ago</h5>
          <h5><%= link_to "Edit Profile", edit_user_path(@user) %></h5>
        </div>
      </div>


      <div class="col-md-8 col-md-offset-1">
    		<div class="col-md-6">
    		<h1 class="text-center">Created</h1>
    			<hr>
    			<% @user.quizzes.each do |quiz| %>
    				<li type="none"><h4 class="quiz-title"><%= link_to quiz.title, quiz_path(quiz.id) %></h4></li>
    				<hr>
    			<% end %>
    		</div>
        <div class='col-md-6'>
    		  <h1 class="text-center">Taken</h1>
    		  <hr>
    		  <% @user.quizzes.each do |quiz| %>
    		  	<li type="none"><h4 class="quiz-title"><%= link_to quiz.title, quiz_path(quiz.id) %></h4></li>
    			 <hr>
    		  <% end %>
    		</div>
      </div>
      
</div>