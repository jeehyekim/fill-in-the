

  <% if @quiz.errors.any? %>
    <% @quiz.errors.full_messages.each do |message| %>
      <div class="errors">
        <ul>
         <li><%= message %></li> 
        </ul>
      </div>
    <% end %>
  <% end %>

<div class="col-sm-6 col-sm-offset-3" id="new-form">
  <h1 class="text-center">Page for new Quiz</h1>
  <%= form_for @quiz do |f| %>
      <%= f.text_field :title, :placeholder => "TITLE...", class: "form-control" %>
      <br>
      <%= f.text_area :content, :placeholder => "CONTENT...", size:"4x15" , class: "form-control" %>
      <br>
      <%= f.text_field :keyword, :placeholder => "KEYWORDS...", class: "form-control" %>
      <br>
      <div class="text-right">
				<%= f.submit "Create", class: "btn", id: "create-quiz-btn" %>
			</div>
  <% end %>
</div>